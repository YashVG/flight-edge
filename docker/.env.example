# ==============================================================================
# FlightEdge Environment Configuration
# Copy this file to .env and customize as needed
# ==============================================================================

# ------------------------------------------------------------------------------
# OpenSky API Credentials (optional)
# Get credentials at: https://opensky-network.org/
# With credentials: 5s poll interval allowed, 4000 credits/day
# Without credentials: 10s poll interval required, 400 credits/day
# ------------------------------------------------------------------------------

# OAuth2 Client Credentials (recommended, required for new accounts)
# Alternatively, place a credentials.json file in the project root
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=

# Basic Auth (legacy, deprecated for new accounts since March 2025)
OPENSKY_USERNAME=
OPENSKY_PASSWORD=

# ------------------------------------------------------------------------------
# Ingestion Settings
# ------------------------------------------------------------------------------
ENABLE_INGESTION=true
POLL_INTERVAL=10s

# ------------------------------------------------------------------------------
# Grafana (if using monitoring profile)
# ------------------------------------------------------------------------------
GRAFANA_PASSWORD=admin

# ------------------------------------------------------------------------------
# Performance Tuning
# Adjust these for your edge hardware constraints
# ------------------------------------------------------------------------------
# GOMAXPROCS - Number of OS threads (default: 1 for edge)
# GOGC - GC target percentage (lower = more frequent GC, less memory)
# GOMEMLIMIT - Soft memory limit (should be ~90% of container limit)
